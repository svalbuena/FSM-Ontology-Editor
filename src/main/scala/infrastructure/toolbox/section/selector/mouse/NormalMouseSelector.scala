package infrastructure.toolbox.section.selector.mouse

import javafx.scene.control.ToggleButton

/**
  * Mouse to create, move and selects elements
  */
class NormalMouseSelector extends ToggleButton {
  setText("Normal mouse")
}
